name: connect_magar_peoples_frontend
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # State Management
  provider: ^6.0.5
  riverpod: ^2.3.6

  # HTTP Requests
  http: ^1.6.0
  dio: ^5.3.2

  # Image caching
  cached_network_image: ^3.4.1

  # Local Storage
  shared_preferences: ^2.2.0

  # Form Validation
  flutter_form_builder: ^9.1.0

  # OTP Input
  pinput: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
